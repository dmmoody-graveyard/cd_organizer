<h1>CD Organizer</h1>

<h2>List of CD's currently in your collection</h2>


<% if @cd == {} %>
  <p><em>You do not have any CD's, please add some.</em></p>
<% else %>
  <ul>
    <% @cd.each() do |k, v| %>
    <li class="cd"><%= k %> - <%= v %></li>
    <% end %>
  </ul>
<% end %>

<h2>Add a new CD to your collection</h2><br />
<form action="/new_cd_entry" method="post">
  <div class="form-group"> q
    <label for="album_name">Album Name: </label>
    <input id="album_name" name="album_name" class="form-control" type="text">
  </div>
  <div class="form-group">
    <label for="artist">Artist: </label>
    <input id="artist" name="artist" class="form-control" type="text">
  </div>
  <button type="submit" class="btn">Add</button>
</form>
